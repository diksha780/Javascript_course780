// *NOTES: => IIFI : Immediately Invoke Function Expressions
// IIFI is used when we want to immediately invoke the declared function 
// => it is used because there is problems caused by global pollution, so to remove or prevent that global pollution like globaly declared variable, IIFI is used